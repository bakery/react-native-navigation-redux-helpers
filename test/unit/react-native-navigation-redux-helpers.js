import reactNativeNavigationReduxHelpers from '../../src/index';

describe('reactNativeNavigationReduxHelpers', () => {
  describe('Greet function', () => {
    beforeEach(() => {
      // spy(reactNativeNavigationReduxHelpers, 'greet');
      // reactNativeNavigationReduxHelpers.greet();
    });

    it('should have been run once', () => {
      expect(true).to.be.ok;
    });

    // it('should have always returned hello', () => {
    //   expect(reactNativeNavigationReduxHelpers.greet).to.have.always.returned('hello');
    // });
  });
});
